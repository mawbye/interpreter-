(define not
    (lambda (bool)
        (if bool #f #t)))

(define flip-flop
  (let ((state #f))
    (lambda ()
      (set! state (not state))
      state)))

(flip-flop)
(flip-flop)
(flip-flop)
(flip-flop)
