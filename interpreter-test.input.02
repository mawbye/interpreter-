(define three
  (lambda ()
    (3)))

(define five
  (lambda ()
    (5)))
    
(define threeOrFive
  (lambda (x)
    (if x (three) (five))))
    
(threeOrFive #t)
(threeOrFive #f)

"TEST CASE NUMBER 2."
